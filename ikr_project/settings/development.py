from .base import *

DEBUG = True
ALLOWED_HOSTS = ['localhost', '127.0.0.1', '127.0.0.1:8000', 'testserver']
# EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'  # Uncomment for console logging
